name: fake_store_roles
description: Fake Store con sesión, roles, catálogo, categorías y detalle de productos.
publish_to: "none"
version: 3.0.0+3

environment:
  sdk: ">=3.13.0 <4.0.0"
  flutter: ">=3.47.2"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  flutter_secure_storage: ^10.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
